import 'package:flutter/material.dart';

class FakeNewsDetectionScreen extends StatelessWidget {
  const FakeNewsDetectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fake News Detection')),
      body: Center(child: Text('Fake News Detection Feature Coming Soon')),
    );
  }
}
