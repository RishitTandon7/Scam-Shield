import 'package:flutter/material.dart';

class PhishingEmailDetectionScreen extends StatelessWidget {
  const PhishingEmailDetectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Phishing Email Detection')),
      body: Center(child: Text('Phishing Email Detection Feature Coming Soon')),
    );
  }
}
