import 'package:flutter/material.dart';

class FinancialFraudMonitorScreen extends StatelessWidget {
  const FinancialFraudMonitorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Financial Fraud Monitor')),
      body: Center(child: Text('Financial Fraud Monitor Feature Coming Soon')),
    );
  }
}
