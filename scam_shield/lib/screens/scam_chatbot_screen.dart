import 'package:flutter/material.dart';

class ScamChatbotScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Scam Chatbot')),
      body: Center(child: Text('Scam Chatbot Feature Coming Soon')),
    );
  }
}
