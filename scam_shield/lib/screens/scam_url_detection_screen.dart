import 'package:flutter/material.dart';

class ScamUrlDetectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Scam URL Detection')),
      body: Center(child: Text('Scam URL Detection Feature Coming Soon')),
    );
  }
}
