import 'package:flutter/material.dart';

class FakeProfileDetectorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fake Profile Detector')),
      body: Center(child: Text('Fake Profile Detector Feature Coming Soon')),
    );
  }
}
