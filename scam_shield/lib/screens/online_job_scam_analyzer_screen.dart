import 'package:flutter/material.dart';

class OnlineJobScamAnalyzerScreen extends StatelessWidget {
  const OnlineJobScamAnalyzerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Online Job Scam Analyzer'),
      ),
      body: const Center(
        child: Text('Online Job Scam Analyzer Feature Coming Soon'),
      ),
    );
  }
}
