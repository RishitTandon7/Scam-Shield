import 'package:flutter/material.dart';

class ImageScamDetectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Image Scam Detection')),
      body: Center(child: Text('Image Scam Detection Feature Coming Soon')),
    );
  }
}
